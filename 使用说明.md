# Telegram 广告限额机器人使用说明

这是基于 [luoyanglang/AI-Anti-Spam-Bot](https://github.com/luoyanglang/AI-Anti-Spam-Bot) 的定制版，保留 MIT 许可证和上游署名。

已完成的规则：

- Kimi Code `k3` 智能识别文字、图片、图片说明和贴纸中的广告。
- 每个群内，每个非管理员成员每个滚动 60 分钟可发 1 条 AI 判定的广告。
- 同一时段第 2 条广告：删除第 2 条，禁言 60 分钟。
- 最近滚动 7×24 小时内第 3 次超额违规：删除消息并永久禁言。
- 群管理员可添加、删除和查看本群屏蔽词。命中屏蔽词会立即删除，不计广告额度，不禁言。
- 群管理员、Bot 超级管理员和配置白名单自动豁免。
- 屏蔽词、广告额度与违规记录均保存在 `data/bot.db`，重启不丢失。

## 一、创建 Telegram Bot

1. 在 Telegram 私聊 `@BotFather`，发送 `/newbot`，按提示创建 Bot。
2. 保存 BotFather 给出的 Token。
3. 在 `@BotFather` 发送 `/setprivacy`，选择刚创建的 Bot，再选择 `Disable`。这是必须步骤，否则 Bot 看不到普通群消息。
4. 在 `/setjoingroups` 中确认允许 Bot 加入群组。

## 二、填写运行密钥

本项目已预设 Kimi Code `k3` 模型和请求地址。复制示例配置：

```bash
cp config.example.yml config.yml
cp .env.example .env
```

然后打开 `.env`，填写 Telegram Bot Token 和你自己的 Kimi Code API Key：

```dotenv
TELEGRAM_BOT_TOKEN=BotFather给你的Token
KIMI_API_KEY=你的Kimi-Code-API-Key
```

当前 `config.yml` 主要配置为：

```yaml
telegram:
  owners: []
  whitelist_user_ids: []
  allow_any_group: true
  groups: []

ai_model: "kimi"

kimi:
  base_url: "https://api.kimi.com/coding/v1"
  model: "k3"

strategy:
  always_check: true
  spam_score: 80

policy:
  ad_interval_minutes: 60
  temporary_mute_minutes: 60
  violation_window_days: 7
  permanent_mute_after: 3
```

`allow_any_group: true` 表示 Bot 被加到哪个群，就在哪个群工作。如果只允许一个群，改成 `false`，并在 `groups` 填入群 ID，例如 `["-1001234567890"]`。

`owners` 可选填写你的 Telegram 数字 ID，用于 `/stats` 和上游的 Bot 广告按钮命令。屏蔽词等群管理命令会直接校验 Telegram 群管理员身份，不依赖 `owners`。

## 三、启动

服务器安装 Docker 和 Docker Compose 后，在项目目录执行：

```bash
docker compose config
docker compose up -d --build
docker compose logs -f bot
```

看到“配置验证通过”和“Bot 启动中”即为正常。按 `Ctrl+C` 只退出日志查看，Bot 仍在运行。

常用命令：

```bash
docker compose restart bot
docker compose logs --tail=200 bot
docker compose down
```

务必定期备份 `data/bot.db`。

## 四、添加到群组

1. 把 Bot 加入群组。
2. 进入“群设置 → 管理员 → 添加管理员”，选择该 Bot。
3. 至少打开“删除消息”和“封禁用户/限制成员”权限。
4. 在群里发送 `/admin`，确认显示 Kimi 模型和“60 分钟 1 条、7 天 3 次永久禁言”规则。

如果 Bot 能回复命令却不检测普通消息，通常是 `/setprivacy` 没有设为 `Disable`。

## 五、群管理命令

- `/admin`：查看当前 AI 模型和广告规则。
- `/ad_status`：回复某成员消息，查看当前额度和滚动窗口违规数。
- `/reset_ad_history`：回复某成员消息，清除额度和违规记录并解除禁言。
- `/unban`：回复某成员消息，解除禁言并清除记录。
- `/add_blockword <词或短语>`：添加本群屏蔽词。例如 `/add_blockword 博彩` 或 `/add_blockword 加我私聊`。
- `/del_blockword <词或短语>`：解除本群屏蔽词。
- `/blockwords`：查看本群全部屏蔽词。

`/ad_status`、`/reset_ad_history` 和 `/unban` 也可带数字 ID，例如 `/ad_status 123456789`。违规通知里的解除按钮也会同时解除禁言和清空记录。

### 屏蔽词的匹配方式

- 按子串匹配；例如屏蔽“博彩”后，“博彩推广”也会命中。
- 不区分英文大小写，并兼容常见全角/半角字符。
- 每个群的词库彼此独立。
- 检查当前文本消息或媒体说明；命中后立即删除并跳过 AI。
- 群管理员和配置白名单的消息不受屏蔽词限制。

## 六、上线前测试

1. 普通账号发送明显推广消息：第 1 条应保留。
2. 一小时内再发一条不同推广消息：第 2 条应删除，账号被禁言 60 分钟。
3. 管理员解除后重复超额操作；最近 7 天第 3 次超额时应永久禁言。
4. 执行 `/add_blockword 测试屏蔽词`，普通成员发送包含该短语的消息，应被立即删除；再执行 `/del_blockword 测试屏蔽词` 即可解除。
5. 重启容器后执行 `/ad_status` 和 `/blockwords`，历史数据应仍然存在。

AI 判定存在误差。`spam_score` 越高越不容易误判，建议先用 `85` 观察几天，漏掉广告较多再降到 `80`。
