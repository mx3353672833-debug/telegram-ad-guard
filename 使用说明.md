# Telegram 广告词/屏蔽词管理机器人

这是基于 [luoyanglang/AI-Anti-Spam-Bot](https://github.com/luoyanglang/AI-Anti-Spam-Bot) 的定制版，保留 MIT 许可证和上游署名。

## 当前默认规则

机器人默认使用确定性关键词模式，不调用任何 AI API。

1. 先检查本群“屏蔽词”。
   - 命中后立即删除原消息。
   - 发言者被永久禁言。
   - 机器人在群里发布处罚通知和“本人发起申诉”按钮。
2. 未命中屏蔽词时，再检查本群“广告词”。
   - 每人在每个群的滚动 60 分钟内可发 1 条广告。
   - 第一条广告保留；同一群内任何账号在 60 分钟内再次发送全文完全相同的广告，也会删除并禁言 60 分钟。
   - 同一时段第 2 条：删除并禁言 60 分钟。
   - 最近 7×24 小时内第 3 次超额：永久禁言，可申诉。
3. 两类词都未命中：直接放行。
4. 同一条消息同时命中两类词时，屏蔽词优先。
5. 群管理员、Bot 超级管理员和白名单用户豁免自动处罚。

词库按子串匹配，不区分英文大小写，兼容常见全角/半角字符。重复广告按当前消息的完整正文或 caption 比较，一个字符不同就视为不同内容；数据库只保存正文哈希，不保存广告明文。每个群的词库独立，保存在 `data/bot.db`，重启不丢失。

## 群管理员命令

### 广告词

```text
/add_adword 源头
/add_adword 低价批发
/del_adword 源头
/adwords
```

### 屏蔽词

```text
/add_blockword 无限开卡
/add_blockword 博彩
/del_blockword 博彩
/blockwords
```

### 手动禁言与解除

- 回复某成员的消息发送 `/mute`：手动永久禁言。
- `/mute <用户ID>`：按 Telegram 数字 ID 永久禁言。
- 回复某成员的消息发送 `/unban`：解除禁言并清除广告额度/违规记录。
- `/unban <用户ID>`：按 ID 解除禁言。
- `/ad_status`：回复成员消息查看其当前广告额度。
- `/reset_ad_history`：回复成员消息，清除记录并解除禁言。

## 申诉流程

1. 永久禁言后，机器人在群里发送带按钮的通知。
2. 只有被禁言者本人能点击“本人发起申诉”。
3. 机器人在群里发布申诉消息。
4. 群管理员点击“同意申诉并解除禁言”，或使用 `/unban`。

永久禁言处罚通知会在 5 分钟后自动删除；被禁言者需在通知删除前点击申诉。已提交的申诉消息会保留等待管理员处理。

## 不使用 AI 时的限制

- 可检查文本消息、图片说明和其他媒体的文字说明。
- 纯图片、纯贴纸或视频画面中的文字不会被视觉识别。
- 如果以后需要识别图片内容，可把 `detection.mode` 改为 `ai`；词库仍会优先执行。

## 配置

```yaml
detection:
  mode: "keywords"

policy:
  ad_interval_minutes: 60
  temporary_mute_minutes: 60
  violation_window_days: 7
  permanent_mute_after: 3
```

`keywords` 模式不需要 Kimi、OpenAI 或其他 AI Key。

## Docker 运行

```bash
docker compose up -d --build
docker compose ps
docker compose logs -f bot
```

机器人需要群管理员的“删除消息”和“封禁用户/限制成员”权限，并需在 BotFather 中关闭 Privacy Mode。
